- **Plan 05-05 (INMET schema-drift fix):** `tests/contract/inmet.test.ts` "Path C invariant" describe block (lines ~174–181) still asserts `src/lib/sources/cemaden.ts` and `cemaden.schema.ts` do NOT exist. Plan 03 (CEMADEN adapter, running in parallel) creates these files legitimately. The invariant must be removed by Plan 03 or Plan 06 (registry wiring) when CEMADEN lands. Plan 05-05 deliberately did not touch it (scope boundary — owns only `inmet*` files).
